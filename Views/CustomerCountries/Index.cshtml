@model IEnumerable<ManufacturaMVC.Dto.CustomerCountriesDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerCountryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCustomerCountry)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerCountryName)
            </td>            
            <td>                
                <a asp-action="Edit" asp-route-id="@item.IdCustomerCountry">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCustomerCountry">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCustomerCountry">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
