@model IEnumerable<ManufacturaMVC.ViewModels.CustomerCountryRegionDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerRegion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerCountry)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerRegion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerCountryId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomerRegion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomerRegion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomerRegion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
